{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install --prefix site",
  "buildCommand": "npm run build --prefix site",
  "outputDirectory": "site/.next",
  "ignoreCommand": "! git log -1 --pretty=%B | grep -q '^chore(dataset):'"
}
